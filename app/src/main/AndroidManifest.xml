<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      联网权限 —— 公告功能必需。
      缺失时不会崩溃，但 URL 请求会抛 SecurityException 被 runCatching 吞掉，
      表现为「公告永远不更新」且无任何提示。
      网络权限属普通权限，声明即可，无需运行时动态申请。
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".KbsApplication"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.KbsPearl">

        <!--
          FileProvider：把崩溃日志以文件形式分享出去。
          Android 7.0+ 禁止跨应用传递 file:// URI，必须走 FileProvider。
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

              <!--
                打开 .txt 配置文件。
                朋友在微信里点开你发的 txt，
                "用其他应用打开" 时能看到本 App。
              -->
              <intent-filter>
                  <action android:name="android.intent.action.VIEW" />
                  <category android:name="android.intent.category.DEFAULT" />
                  <data android:mimeType="text/plain" />
              </intent-filter>

              <!--
                其他 App 分享文本/文件给本 App。
                例如从文件管理器里"分享"一个 txt。
              -->
              <intent-filter>
                  <action android:name="android.intent.action.SEND" />
                  <category android:name="android.intent.category.DEFAULT" />
                  <data android:mimeType="text/plain" />
              </intent-filter>
        </activity>

        <!--
          崩溃日志查看页。
          ★ 不配 intent-filter —— 它不出现在桌面，
            只能由 MainActivity 在检测到崩溃记录时启动。
        -->
        <activity
            android:name=".ui.CrashActivity"
            android:exported="false"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar" />
    </application>

</manifest>
